[package]
name = "unrot_cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "unrot"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
unrot_core = { path = "../core" }
